{% assign metaf = product.metafields.c_f %}
<style>
    .site-nav > li > a{
        padding: 21px 22px;
    }
    .header-tool {
        padding: 18px;
    }
    .logo{
        padding-top: 9px;
    }
    .breadcrumb{
        margin-top: 5px;
    }
    .flex-control-thumbs li{
        margin: 0 5px 0 0 !important;
        width: 70px !important;
        min-height: 51px !important;
    }
    .flex-control-thumbs li:first-child{
        margin-left: 0 !important;
    }



    .product-shop .product-title h1, .product-shop .product-title a{
        font-size: 32px !important;
        line-height: 48px;
        font-style: normal;
        letter-spacing: 1px;
    }
    .product-shop .product-title{
        margin-bottom: 0;
    }
    @media (min-width: 1200px){
        .product-shop {
            max-width: auto;
        }
    }
    .yotpo {
        margin-bottom: 10px;
    }
    .product-shop .short-description {
        line-height: 25px;
        letter-spacing: 1px;
        color: #666;
    }
    #product-variants {
        margin-bottom: 10px;
    }
    div.addonmodule {
        padding: 10px;
    }
    .squaredOne {
        margin: 5px auto;
    }
    div.addonmodule .column.img {
        padding: 0 15px 0 20px;
        width: 95px;
    }
    DIV.cta-module {
        margin: 20px 0 10px 0;
    }
    .cta2 {
        margin-top: 10px;
    }
    DIV.affirm A {
        line-height: 14px;
    }
    DIV.cta-module {
        margin-top: 20px !important;
    }
    .cta2 {
        margin-top: 10px;
    }
    @media (min-width: 1024px){
        .buycontent {
            /*margin-left: 6%;*/
        }
    }
    @media (max-width: 768px){
        .header-tool {
            padding: 0px 18px 0 0;
        }
    }
    @media (max-width: 991px) and (min-width: 768px){
        .header.is-ticky .site-nav > li > a {
            padding: 20px 5px 20px;
        }
        .flex-control-thumbs li{
            width: 60px !important;
            height: 44px !important;
            overflow: hidden;
        }
        .header-tool {
            padding: 10px;
        }
        .header.is-ticky #search-top .icon_search, .header.is-ticky #header-content .header-inner p.top-cart {
            line-height: 18px;
        }
        .product-shop>DIV {
            margin: 0;
        }
        #page_product .product-shop h1{
            text-align: left;
            font-size: 32px !important;
        }
    }
</style>

<div id="page_product">
    <div class="container-fluid" id="product">
        <div itemscope itemtype="http://schema.org/Product" class="product">
            <meta itemprop="url" content="{{ shop.url }}{{ product.url }}">
            <meta itemprop="image"
                  src="{{ product.featured_image.src | product_img_url: 'grande' }}"
                  alt="{{ product.title | escape }}
                       ">
            <meta itemprop="itemCondition" content="http://schema.org/NewCondition"/>
            <div class="row">
                <style>
                    .flexslider .slides > li{
                        background-color: #f9f9f9;
                    }
                    .embed-container { 
                        position: relative;
                        left: 0; 
                        right: 0;
                        top: 50%;
                        /* video height / video width */
                        padding-bottom: 66.2%;
                        /* the above value * 0.5 */
                        /*margin-top: -28.1%;*/
                        height: 0;
                        overflow: hidden;
                    } 
                    .embed-container iframe, .embed-container object, .embed-container embed { 
                        position: absolute; 
                        top: 0; 
                        left: 0; 
                        width: 100%; 
                        height: 100%; 
                    }
                </style>
                <!-- FLEXSLIDER -->
                {% include 'product-flexslider' %}
                
                {% assign multi_prd = true %}
                {% if collection.products_count <= 1 %}
                                                   {% assign multi_prd = false %}
                                                   {% endif %}
                                                   <div class="col-xs-12 col-md-5 col-lg-4 product-shop">

                <div>
                    {% if settings.display_prev_next_product and collection and multi_prd %}
                    <div class="product-step">
                        {% if collection.previous_product %}
                        <a class="prev-p" href="{{ collection.next_product }}"
                           title="Prev"><span
                                              class="fa fa-angle-left"></span>Previous</a>
                        {% endif %}
                        {% if collection.next_product %}
                        <a class="next-p" href="{{ collection.next_product }}"
                           title="Next">Next<span
                                                  class=" fa fa-angle-right"></span></a>
                        {% endif %}
                    </div>
                    {% else %}
                    <div class="clearfix"></div>
                    {% endif %}
                    <header class="product-title {% if collection.previous_product or collection.next_product and settings.display_prev_next_product and collection %}has-btn{% endif %}">

                        <h1 {% if settings.display_prev_next_product and collection and multi_prd %}class="pull-left"{% endif %}
                            itemprop="name">{{ product.title }}</h1>

                    </header>

                    <div style="margin-bottom: 20px;" class="yotpo bottomLine"
                         data-appkey="MoTNJx7IZaWhweNlLkQdd6AaJffubtZuHPH6w3aQ"
                         data-domain="{{ shop.permanent_domain | escape }}"
                         data-product-id="{{ product.id }}"
                         data-product-models="{{ product.id }}"
                         data-name="{{ product.title | escape }}"
                         data-url="{{ shop.url }}{{ product.url }}"
                         data-image-url="{{ product.featured_image | product_img_url: 'large' |replace: '?', '%3F' | replace: '&','%26' }}"
                         data-description="{{ product.description | escape }}"
                         data-bread-crumbs="{% for tag in product.tags %}{{ tag | escape }};{% endfor %}" itemprop="aggregateRating" itemscope itemtype="http://schema.org/AggregateRating">
                        <meta itemprop="itemReviewed"
                              content="{{ product.title }}">
                        <meta itemprop="ratingValue"
                              content="{{ product.metafields.yotpo.reviews_average }}">
                        <meta itemprop="ratingCount"
                              content="{{ product.metafields.yotpo.reviews_count }}">
                    </div>

                    {% include 'nowtime' %}
                    <p class="short-description">{{ metaf.shortdesc }}</p>

                    <div class="mob-dark">
                        {% if settings.display_product_reviews %}
                        <span class="shopify-product-reviews-badge"
                              data-id="{{ product.id }}"></span>
                        {% endif %}
                        {% if settings.display_availability or settings.display_product_type or settings.display_vendor %}
                        {% if settings.display_availability %}
                        <div class="availability">
                            <p class="product-inventory">
                                <label>Availability</label><span>
                                {% if product.selected_or_first_available_variant.inventory_management %}
                                {% assign first_inventory = product.selected_or_first_available_variant.inventory_quantity %}
                                {% if first_inventory > 0 %}
                                {{ first_inventory }} in stock
                                {% else %}
                                Out of stock
                                {% endif %}
                                {% else %}
                                Many in stock
                                {% endif %}
                                </span></p>
                        </div>
                        {% endif %}
                        {% endif %}
                        <form action="/cart/add" method="post" enctype="multipart/form-data" id="add-to-cart-form">
                            <label for="product-selectors" class="selectLabel">Size</label>
                            <div id="product-variants">
                                {% assign variantCount = product.variants | size %}
                                {% if product.available %}
                                {% if variantCount > 1 %}
                                <select id="product-selectors" name="id"
                                        style="display:none">
                                    {% for variant in product.variants %}
                                    <option {% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}
                                            value="{{ variant.id }}">{{ variant.title }}
                                        - {{ variant.price | money }}</option>
                                    {% endfor %}
                                </select>
                                {% if settings.use_color_swatch %}
                                {% for option in product.options %}
                                {% include 'swatch' with option %}
                                {% endfor %}
                                {% endif %}
                                {% else %}
                                <input type="hidden" name="id" value="{{ product.variants[0].id }}"/>
                                {% endif %}
                                {% endif %}
                            </div>
                            {% if product.available %}
                            <div class="cta-module clearfix">
                                <div class="prices" itemprop="offers" itemscope itemtype="http://schema.org/Offer" style="float: left;">
                                    <meta itemprop="priceCurrency" content="{{ shop.currency }}">
                                    {% if product.compare_at_price > product.price %}
                                    <span class="compare-price">{{ product.compare_at_price_max | money }}</span>
                                    <span class="price on-sale" itemprop="price"
                                          content="{{ product.selected_or_first_available_variant.price | money_without_currency | remove: ',' }}">{{ product.selected_or_first_available_variant.price | money_without_trailing_zeros}}</span>
                                    {% else %}
                                    <span class="price" itemprop="price"
                                          content="{{ product.selected_or_first_available_variant.price | money_without_currency | remove: ',' }}">{{ product.selected_or_first_available_variant.price | money_without_trailing_zeros }}</span>
                                    {% endif %}
                                    {% if product.available %}
                                    <link itemprop="availability" href="http://schema.org/InStock">
                                    {% else %}
                                    <link itemprop="availability" href="http://schema.org/OutOfStock">
                                    {% endif %}
                                </div>



                                {% include 'affirm-product' %}
                                <style>
                                    DIV.cta-module{
                                        margin: 10px 0;
                                    }
                                    DIV.product-shop .prices{
                                        width: 49%;
                                        text-align: center;
                                        padding-top: 0px;
                                        border-right: 1px solid black;
                                    }
                                    DIV.prices SPAN.price{
                                        font-size: 32px;
                                        display: block;
                                    }
                                    DIV.affirmWrap{
                                        float: right;
                                        width: 49%;
                                        text-align: center;
                                        padding: 0 8px;
                                    }
                                    DIV.affirm{
                                        font-size: 12px;
                                        text-align: center;
                                    }
                                    DIV.affirm A#learn-more{
                                        margin-bottom: 0px;
                                        line-height: 18px;
                                        text-decoration: none;
                                    }
                                    DIV.affirm A#learn-more:HOVER{
                                        margin-bottom: 0px;
                                        line-height: 18px;
                                        text-decoration: none;
                                    }
                                    DIV.affirm A.affirm-modal-trigger{
                                        cursor: pointer;
                                        display: inline-block;
                                        text-decoration: none;
                                        background-color: #0FA0EA;
                                        color: white;
                                        padding: 5px 9px;
                                        border-radius: 5px;
                                        line-height: 14px;
                                        text-transform: capitalize;
                                    }
                                    DIV.affirm A.affirm-modal-trigger:HOVER{
                                        text-decoration: none;
                                        background-color: transparent;
                                        color: #0FA0EA;

                                    }
                                </style>
                                <div class="affirmWrap">
                                    <div class="affirm" style="">
                                        <a id="learn-more" class="affirm-as-low-as" data-learnmore-show="true" data-promo-id="promo_set_brentwood_zero_percent" data-amount="{{variant.price}}"></a>
                                    </div>
                                </div>

                                <div id="qt" style="display: none">
                                    QTY: <input type="text" id="quantity" name="quantity" value="1">
                                </div>
                            </div>
                                {% include 'shippingtime' %}
                                <button type="submit" name="add" class="cta2" id="product-add-to-cart">Add to Cart</button>
                                <p id="add2cart_shipping"><img src="https://brentwood-home.imgix.net/iconv2-freeshipping.svg" alt="Ships free in {{ shipsfree }}">Ships free in {{ shipsfree }}</p>
                            {% else %}
                                <input type="submit" name="add" class="btn" id="product-add-to-cart" disabled value="Unavailable">
                            {% endif %}

                            {% assign current_variant = product.selected_or_first_available_variant %}
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div><!-- End Row --> 
</div><!-- End Container for top part (img+buy) -->
<div class="product-tabs container">
   
    {% if metaf.product_tagline or metaf.story %}
        <div class="overlays overlays-2">
            <h2 class="overlays-title">
                {{ metaf.product_tagline }}
            </h2>
            <div class="row">
                {% if metaf.story_video %}
                    <div class="content col-xs-12 col-md-6">
                        <p>{{ metaf.story }}</p>
                    </div>
                    <div class="content col-xs-12 col-md-6">
                        <div class="text-center videoWrapper">
                            {% if metaf.story_video %}
                            {{ metaf.story_video }}
                            {% endif %}
                        </div>
                    </div>
                {% else %}
                    <div class="content col-xs-12 col-md-10 col-md-offset-1">
                        <p>{{ metaf.story }}</p>
                    </div>
                {% endif %}
            </div>
        </div>
    {% endif %}
    
    {% if product.type == "Box Foundation" %}
        <div class="container section warranties">
            <h2 class="text-center margTB50">Try {{ product.title }} Risk-Free</h2>
            <div class="row">
                <div class="col-xs-3 col-sm-3">
                    <img src="https://brentwood-home-2.imgix.net/iconv2-1yearguarantee90.svg" alt="1-Year Trial">
                    <p>1-Year<br>Trial</p>
                </div>
                <div class="col-xs-3 col-sm-3">
                    <img src="https://brentwood-home-2.imgix.net/iconv2-freeshipping90.svg" alt="Free Shipping Free Return">
                    <p>Free Shipping<br>Free Return</p>
                </div>
                <div class="col-xs-3 col-sm-3">
                    <img src="https://brentwood-home-2.imgix.net/iconv2-5yearwarranty.svg" alt="5-year Warranty">
                    <p>5-year<br>Warranty</p>
                </div>
                <div class="col-xs-3 col-sm-3">
                    <img src="https://brentwood-home.imgix.net/iconv2-0percent.svg" alt="0% Financing Available">
                    <p>0% Financing<br>Available</p>
                </div>
            </div>
        </div>
    {% else %}
        <div class="overlays overlays-1" style="margin: 50px 0;">
            <h2 class="overlays-title">Peace of mind guarantee</h2>
            <div class="items row">
                <div class="item col-xs-6 col-md-3">
                    <img src="{{ 'icon-FreeShipping2.svg' | asset_url }}"/>
                    <br/>
                    <label>Free Shipping</label>
                </div>
                <div class="item col-xs-6 col-md-3">
                    <img src="{{ 'icon-FreeReturns.svg' | asset_url }}"/>
                    <br/>
                    <label>Free Returns</label>
                </div>
                <div class="item col-xs-6 col-md-3">
                    <img src="{{ 'icon-California.svg' | asset_url }}"/>
                    <br/>
                    <label>Made in California</label>
                </div>
                <div class="item col-xs-6 col-md-3">
                    <img src="{{ 'icon-3YearWarranty.svg' | asset_url }}"/>
                    <br/>
                    <label>3-year Warranty</label>
                </div>
            </div>
        </div>
    {% endif %}
    <br>
    {% if metaf.feature1_title %}
    <section class="overlays overlays-5 container">
        <div class="row">
            <div class="col-xs-12 col-sm-6">
                <img loading="lazy" data-src="https://brentwood-home-2.imgix.net/{{ metaf.feature1_img }}" src="https://brentwood-home-2.imgix.net/{{ metaf.feature1_img }}?blur=500&q=10" alt="{{metaf.feature1_title | escape }}" class="feature-img img-responsive img">
            </div>
            <div class="col-xs-12 col-sm-6">
                <h2 class="overlays-title text-center">{{metaf.feature1_title}}</h2>
                <p>{{metaf.feature1}}</p>
                <div class="row">
                    {{ metaf.feature1_icons }}
                </div>
            </div>
        </div>
    </section>
    {% endif %}
    {% if metaf.feature2_title %}
    <section class="overlays overlays-5 container">
        <div class="row">
            <div class="col-xs-12 col-sm-6 col-sm-push-6">
                <img loading="lazy" data-src="https://brentwood-home-2.imgix.net/{{ metaf.feature2_img }}" src="https://brentwood-home-2.imgix.net/{{ metaf.feature2_img }}?blur=500&q=10" alt="{{metaf.feature2_title | escape }}" class="feature-img img-responsive img">
            </div>
            <div class="col-xs-12 col-sm-6 col-sm-pull-6">
                <h2 class="overlays-title text-center">{{metaf.feature2_title}}</h2>
                <p>{{metaf.feature2}}</p>
                <div class="row">
                    {{ metaf.feature2_icon }}
                </div>
            </div>
        </div>
    </section>
    {% endif %}

    {% if metaf.feature3_title %}
    <section class="overlays overlays-6 container">
        <div class="row">
            <div class="col-xs-12 col-sm-6">
                {% if metaf.feature3_img contains "-video" %}
                    <div class="videoWrapper2 vimeo">
                        <video playsinline autoplay loop muted poster="https://brentwood-home-2.imgix.net/{{ metaf.feature3_img }}.jpg" preload="none">
                            <source src="https://s3.us-east-2.amazonaws.com/brentwoodhome/imgix/{{ metaf.feature3_img }}.mp4" type="video/mp4">
                            <source src="https://s3.us-east-2.amazonaws.com/brentwoodhome/imgix/{{ metaf.feature3_img }}.webm" type='video/webm; codecs="vp8, vorbis"' />
                            <img alt="{{metaf.feature3_title | escape }}" src="https://brentwood-home-2.imgix.net/{{ metaf.feature3_img }}.jpg">
                        </video>
                    </div>
                {% else %}
                <img loading="lazy" data-src="https://brentwood-home-2.imgix.net/{{ metaf.feature3_img }}" src="https://brentwood-home-2.imgix.net/{{ metaf.feature3_img }}?blur=500&q=10" alt="{{metaf.feature3_title | escape }}" class="feature-img img-responsive img">
                {% endif %}
            </div>
            <div class="col-xs-12 col-sm-6">
                <h2 class="overlays-title text-center">{{metaf.feature3_title}}</h2>
                <p>{{metaf.feature3}}</p>
                <div class="row">
                    {{ metaf.feature3_icons }}
                </div>
            </div>
        </div>
    </section>
    {% endif %}

    {% if metaf.feature4_title %}
    <section class="overlays overlays-5 container">
        <div class="row">
            <div class="col-xs-12 col-sm-6 col-sm-push-6">
                <img loading="lazy" data-src="https://brentwood-home-2.imgix.net/{{ metaf.feature4_img }}" src="https://brentwood-home-2.imgix.net/{{ metaf.feature4_img }}?blur=500&q=10" alt="{{metaf.feature4_title | escape }}" class="feature-img img-responsive img">
            </div>
            <div class="col-xs-12 col-sm-6 col-sm-pull-6">
                <h2 class="overlays-title text-center">{{metaf.feature4_title}}</h2>
                <p>{{metaf.feature4}}</p>
                <div class="row">
                    {{ metaf.feature4_icons }}
                </div>
            </div>
        </div>
    </section>
    {% endif %}

    {% if metaf.feature5_title %}
    <section class="overlays overlays-5 container">
        <div class="row">
            <div class="col-xs-12 col-sm-6">
                <img loading="lazy" data-src="https://brentwood-home-2.imgix.net/{{ metaf.feature5_img }}" src="https://brentwood-home-2.imgix.net/{{ metaf.feature5_img }}?blur=500&q=10" alt="{{metaf.feature5_title | escape }}" class="feature-img img-responsive img">
            </div>
            <div class="col-xs-12 col-sm-6">
                <h2 class="overlays-title text-center">{{metaf.feature5_title}}</h2>
                <p>{{metaf.feature5}}</p>
                <div class="row">
                    {{ metaf.feature5_icons }}
                </div>
            </div>
        </div>
    </section>
    {% endif %}

    {% if metaf.ecommimage_tagline %}
        <section class="overlays overlays-7 only-desktop">
            <img class="col-xs-12" src="{{ metaf.ecommimage | asset_url }}"/>
            <p>
                {{ metaf.ecommimage_tagline }}
            </p>
        </section>
    {% endif %}
    
    {% if product.type == "Pet Bed" %}
        <section class="overlays overlays-11">
            <h2 class="overlays-title">
                What people say about us
            </h2>
            <div class="container sectionMarg">
                <div id="home-reviews" class=" owl-carousel owl-theme">
                    <div class="item text-center">
                        <p class="text-center">
                            <i>I have spent thousands on Pet beds in the 8 years we've had our precious golden retriever. For a while we always purchased the large pet couches from Frontgate and Orvis. I have to say this has been the best bed.</i>
                        </p>
                        <p class="author">
                            - Tiffany H.
                            <i class="fa fa-star" aria-hidden="true"></i><i class="fa fa-star" aria-hidden="true"></i><i class="fa fa-star" aria-hidden="true"></i><i class="fa fa-star" aria-hidden="true"></i><i class="fa fa-star" aria-hidden="true"></i>
                        </p>
                    </div>
                    <div class="item text-center">
                        <p class="text-center">
                            <i>We bought two of these for our three huskies. Fit well style-wise in our living room. The fabric cleans very well and people comment on how nice it is.</i>
                        </p>
                        <p class="author">
                            - Joseph T.
                            <i class="fa fa-star" aria-hidden="true"></i><i class="fa fa-star" aria-hidden="true"></i><i class="fa fa-star" aria-hidden="true"></i><i class="fa fa-star" aria-hidden="true"></i><i class="fa fa-star" aria-hidden="true"></i>
                        </p>
                    </div>
                    <div class="item text-center">
                        <p class="text-center">
                            <i>I purchased this bed for my dog, she has knee problems and has had surgery. She would always limp when she got up from her other bed. With this bed, she does so much better and doesn't limp when getting up from this bed.</i>
                        </p>
                        <p class="author">
                            - Gina S.
                            <i class="fa fa-star" aria-hidden="true"></i><i class="fa fa-star" aria-hidden="true"></i><i class="fa fa-star" aria-hidden="true"></i><i class="fa fa-star" aria-hidden="true"></i><i class="fa fa-star" aria-hidden="true"></i>
                        </p>
                    </div>
                </div>
            </div>
        </section>
    {% endif %}
    <script>
        $(document).ready(function() {
            $("#home-reviews").owlCarousel({
                loop:false,
                margin:10,
                responsiveClass:true,
                responsive:{
                    0:{
                        items:1,
                        //nav:true
                    },
                    768:{
                        items:2,
                        //nav:true
                    },
                    1024:{
                        items:3,
                        //nav:true
                    }
                },
                nav : false
            });
        });
    </script>

  
    {% if product.type == "Box Foundation" %}
        <section class="overlays overlays-5 container">
            <div class="row">
                <div class="col-xs-12 col-sm-10 col-sm-offset-1">
                    <h2 class="overlays-title text-center" style="text-align: center;">From Box to Foundation in Minutes</h2>
                    <div class="embed-container ">
                        <!--https://player.vimeo.com/video/###-->
                        <iframe src="https://player.vimeo.com/video/315913679?title=0&amp;byline=0&amp;portrait=0" width="640" height="360" id="video_1" frameborder="0"></iframe>
                    </div>
                </div>
            </div>
        </section>
    {% endif %}
   
    <section class="overlays overlays-tabs container">
        <div id="exTab1" class="">
        <h2 class="overlays-title">Details</h2>
            <ul class="nav nav-pills">
                <li class="active">
                    <a href="#1a" data-toggle="tab">Description</a>
                </li>
                <li>
                    <a href="#2a" data-toggle="tab">Materials</a>
                </li>
                <li>
                    <a href="#3a" data-toggle="tab">Sizes</a>
                </li>
                <li>
                    <a href="#4a" data-toggle="tab">Shipping & Returns</a>
                </li>
                {%comment%}
                <li>
                    <a href="#5a" data-toggle="tab">Certifications</a>
                </li>
                {%endcomment%}
            </ul>
            <div class="tab-content clearfix">
                <div class="tab-pane active" id="1a">
                    <div class="description text-left">
                        {{ product.description }}
                    </div>
                </div>
                <div class="tab-pane" id="2a">
                    <div class="description text-left">
                        {{ metaf.details_materials }}
                    </div>
                </div>
                <div class="tab-pane" id="3a">
                    <div class="description">
                        <div class="container">
                            <div class="row">
                                {{ metaf.details_sizes }}
                            </div>
                        </div>
                    </div>
                </div>
                <div class="tab-pane" id="4a">
                    <div class="description">
                        <div class="container">
                            <div class="row">
                                {% if product.type == "Pet Bed" %}
                                <div class="col-xs-12 col-sm-6">
                                    <p><strong>Free Standard Shipping:</strong><br>
                                        This product is handcrafted to order in approximately 5-7 business days. Once your purchase ships, we’ll send you an email with tracking information so you can follow your shipment and plan for delivery.</p>
                                        {% include 'shippingtime' %}
                                </div>

                                <div class="col-xs-12 col-sm-6">
                                    <p><strong>Returns:</strong><br>
                                        We offer free returns for 30 days with our pet beds.</p>
                                </div>
                                {% else %}
                                {% if shipsfree %}
                                    <p class="text-left"><strong>Note: Currently shipping out for free in {{ shipsfree }}.</strong></p>
                                {% endif %}
                                    {{ metaf.details_shipping }}
                                {% endif %}
                            </div>
                        </div>
                    </div>
                </div>
                {%comment%}
                <div class="tab-pane" id="5a">
                    <div class="description">description for [Certifications]</div>
                </div>{%endcomment%}
            </div>
        </div>
    </section>
    {%comment%}
    <section class="overlays overlays-13 only-mobile">
        <div class="tabs">
            <ul class="nav nav-pills">
                <li class="active">
                    <a href="#m1a" data-toggle="tab">
                        Description
                        <img src="{{ 'arrow@2x.svg' | asset_url }}" alt="" class="arrow">
                    </a>
                    <div class="tab-pane active" id="m1a">
                        <div class="description">{{ product.description }}</div>
                    </div>
                </li>
                <li>
                    <a href="#m2a" data-toggle="tab">
                        Materials
                        <img src="{{ 'arrow@2x.svg' | asset_url }}" alt="" class="arrow">
                    </a>
                    <div class="tab-pane" id="m2a">
                        <div class="description">description for [Materials]</div>
                    </div>
                </li>
                <li>
                    <a href="#m3a" data-toggle="tab">
                        Sizes
                        <img src="{{ 'arrow@2x.svg' | asset_url }}" alt="" class="arrow">
                    </a>
                    <div class="tab-pane" id="m3a">
                        <div class="description">description for [Sizes]</div>
                    </div>
                </li>
                <li>
                    <a href="#m4a" data-toggle="tab">
                        Shipping & Returns
                        <img src="{{ 'arrow@2x.svg' | asset_url }}" alt="" class="arrow">
                    </a>
                    <div class="tab-pane" id="m4a">
                        <div class="description">description for [Shipping & Returns]</div>
                    </div>
                </li>
                <li>
                    <a href="#m5a" data-toggle="tab">
                        Certifications
                        <img src="{{ 'arrow@2x.svg' | asset_url }}" alt="" class="arrow">
                    </a>
                    <div class="tab-pane" id="m5a">
                        <div class="description">description for [Certifications]</div>
                    </div>
                </li>
            </ul>
        </div>
    </section>
    {%endcomment%}

    {% comment %}
    <br><br>
    {% include 'mattress-platforms' %}
    <ul class="nav nav-tabs" id="myTab">
        {% if settings.display_product_detail_description %}
        <li class="active"><a data-toggle="tab" href="#collapse-tab1">{{ settings.product_description_title }}</a></li>
        {% endif %}
        {% if settings.display_product_reviews %}
        <li><a data-toggle="tab"
               href="#collapse-tab2">{{ settings.product_review_title }}</a>
        </li>
        {% endif %}
    </ul>
    <div id="myTabContent" class="tab-content">
        {% if settings.display_product_detail_description %}
        <div id="collapse-tab1" class="tab-pane fade in active">
            {{ product.description }}
        </div>
        {% endif %}
        {% if settings.display_product_reviews %}
        <div id="collapse-tab2" class="tab-pane fade">
            {% comment %}
            Add product review widget, must install Product Review App:
            https://apps.shopify.com/product-reviews
            {% endcomment %}
            <div id="shopify-product-reviews"
                 data-id="{{ product.id }}">{{ product.metafields.spr.reviews }}</div>
        </div>
        {% endif %}
    </div>
    {% endcomment %}
</div>
<script>
    jQuery('#myTab a').click(function (e) {
        e.preventDefault();
        jQuery(this).tab('show');
    })
</script>
{{ 'option_selection.js' | shopify_asset_url | script_tag }}
{% include 'block-productdetail-right' %}
<script>
    jQuery(document).ready(function () {
        jQuery("#zt_list_product").owlCarousel({

            loop:false,
            margin:10,
            responsiveClass:true,
            responsive:{
                600:{
                    items:3,
                    //nav:true
                }
            },
            nav : false
        });
    });
    //Shopify.Image.preload({{ product.images | json }}, 'grande');

    {% if settings.display_subtotal and product.available %}
    //update price when changing quantity
    function updatePricing() {
        //try pattern one before pattern 2
        var regex = /([0-9]+[.|,][0-9]+[.|,][0-9]+)/g;
        var unitPriceTextMatch = jQuery('.product .price').text().match(regex);

        if (!unitPriceTextMatch) {
            regex = /([0-9]+[.|,][0-9]+)/g;
            unitPriceTextMatch = jQuery('.product .price').text().match(regex);
        }

        if (unitPriceTextMatch) {
            var unitPriceText = unitPriceTextMatch[0];
            var unitPrice = unitPriceText.replace(/[.|,]/g, '');
            var quantity = parseInt(jQuery('#quantity').val());
            var totalPrice = unitPrice * quantity;

            var totalPriceText = Shopify.formatMoney(totalPrice, window.money_format);

            totalPriceText = totalPriceText.match(regex)[0];

            var regInput = new RegExp(unitPriceText, "g");
            var totalPriceHtml = jQuery('.product .price').html().replace(regInput, totalPriceText);

            jQuery('.product .total-price span').html(totalPriceHtml);
        }
    }

     jQuery('#quantity').on('change', updatePricing);
     {% endif %}

     var selectCallback = function (variant, selector) {

         var addToCart = jQuery('#product-add-to-cart'),
             productPrice = jQuery('.product .price'),
             comparePrice = jQuery('.product .compare-price');

         if (variant) {
             if (variant.available) {
                 // We have a valid product variant, so enable the submit button
                 addToCart.removeClass('disabled').removeAttr('disabled').val('Add to Cart');

             } else {
                 // Variant is sold out, disable the submit button
                 addToCart.val('Sold Out').addClass('disabled').attr('disabled', 'disabled');
             }

             // Regardless of stock, update the product price
             productPrice.html(Shopify.formatMoney(variant.price, "{{ shop.money_format }}"));

             // Also update and show the product's compare price if necessary
             if (variant.compare_at_price > variant.price) {
                 productPrice.addClass("on-sale")
                 comparePrice
                     .html(Shopify.formatMoney(variant.compare_at_price, "{{ shop.money_format }}"))
                     .show();
             } else {
                 comparePrice.hide();
                 productPrice.removeClass("on-sale");
             }
             {% if settings.use_color_swatch %}
             // BEGIN SWATCHES
             var form = jQuery('#' + selector.domIdPrefix).closest('form');
              for (var i = 0, length = variant.options.length; i < length; i++) {
                  var radioButton = form.find('.swatch[data-option-index="' + i + '"] :radio[value="' + variant.options[i] + '"]');
                  if (radioButton.size()) {
                      radioButton.get(0).checked = true;
                  }
              }
              // END SWATCHES
              {% endif %}

              {% if settings.display_subtotal and product.available %}
              updatePricing();
               {% endif %}


               } else {
                   // The variant doesn't exist. Just a safeguard for errors, but disable the submit button anyway
                   addToCart.val('Unavailable').addClass('disabled').attr('disabled', 'disabled');
               }

               //update variant inventory
               {% if settings.display_availability %}
               if (variant.available) {
                   if (variant.inventory_management != null) {
                       jQuery(".product-inventory span").text(variant.inventory_quantity + " in stock");
                   } else {
                       jQuery(".product-inventory span").text("Many in stock");
                   }
               } else {
                   jQuery(".product-inventory span").text("Out of stock");
               }
                {% endif %}

                //display message for sheets and quilts when out of stock
                if (variant.available) {
                    if (variant.inventory_management != null) {
                        if (variant.inventory_quantity < 1) {
                            jQuery(".out-of-stock").show();
                            jQuery(".out-of-stock p").html("We’re currently making this item to order.<br> Please allow <b>up to 3 weeks</b> for crafting and shipping.");
                        } else {
                            jQuery(".out-of-stock").hide();
                        }
                    }
                } else {
                    jQuery(".out-of-stock p").text("Out of stock");
                }

                 /*begin variant image*/
                 if (variant && variant.featured_image) {
                     var originalImage = jQuery("#product-featured-image");
                     var newImage = variant.featured_image;
                     var element = originalImage[0];
                     Shopify.Image.switchImage(newImage, element, function (newImageSizedSrc, newImage, element) {
                         jQuery('#zt_list_product img').each(function () {
                             var grandSize = jQuery(this).attr('src');
                             grandSize = grandSize.replace('compact', 'grande');
                             if (grandSize == newImageSizedSrc) {
                                 console.log(newImageSizedSrc);
                                 jQuery(this).parent().trigger('click');
                                 return false;
                             }
                         });
                     });
                 }
                 /*end of variant image*/
                 //updateAffirmAsLowAs(variant.price); //Update affirm price
                 $('.affirm-as-low-as').attr('data-amount',variant.price);
                 affirm.ui.ready(function(){
                     affirm.ui.refresh(); 
                 });
                };

                jQuery(function ($) {
                    {% if product.available and product.variants.size > 1 %}
                    new Shopify.OptionSelectors('product-selectors', {
                        product: {{ product | json }},
                                                onVariantSelected: selectCallback,
                                                enableHistoryState: true
                                                });

                     {% comment %}
                     Use
                     color
                     swatch
                     and
                     linked
                     options(copyright
                             by
                             @carolineschnapp)
                      {% endcomment %}
                      {% if settings.use_color_swatch and product.available and product.options.size > 1 %}
                      Shopify.linkOptionSelectors({{ product | json }});
                       {% endif %}

                       {% if settings.use_color_swatch != true %}
                       jQuery('.single-option-selector').selectize();
                        jQuery('.selectize-input input').attr("disabled", "disabled");
                        {% endif %}

                        {% endif %}

                        // Add label if only one product option and it isn't 'Title'. Could be 'Size'.
                        {% if product.options.size == 1 and product.options.first != 'Title' %}
                        $('.selector-wrapper:eq(0)').prepend('<label>{{ product.options.first }}</label>');
                         {% endif %}

                         // Hide selectors if we only have 1 variant and its title contains 'Default'.
                         {% if product.variants.size == 1 and product.variants.first.title contains 'Default' %}
                         $('.selector-wrapper').hide();
                          {% endif %}

                          // Auto-select first available variant on page load. Otherwise the product looks sold out.
                          {% assign found_one_in_stock = false %}
                          {% for variant in product.variants %}
                          {% if variant.available and found_one_in_stock == false %}
                          {% assign found_one_in_stock = true %}
                          {% for option in product.options %}
                          $('.single-option-selector:eq({{ forloop.index0 }})').val({{ variant.options[forloop.index0] | json }}).trigger('change');
                           {% endfor %}
                           {% endif %}
                           {% endfor %}

                           {% if settings.display_product_reviews %}
                           var reviewsTimeout = setInterval(function () {
                               if (jQuery(".spr-badge-caption").length > 0) {
                                   jQuery(".spr-badge-caption").on('click', function () {
                                       jQuery('html,body').animate({
                                           scrollTop: jQuery(".panel:last").offset().top
                                       },
                                                                   '400');
                                       jQuery("#collapse-tab4").collapse('show');
                                   });
                                   clearInterval(reviewsTimeout);
                               }
                           }, 1000);

                            {% endif %}

                            });

</script>

<div id="yotpoReviews" class="container margTB120">
    <div class="row">
        <div class="col-lg-12 col-md-12 col-sm-12 col-sx-12 text-center">
            <h2 class="text-center">Reviews</h2>
        </div>
        <div class="col-xs-12">
            <div class="yotpo yotpo-main-widget"
                 data-product-id="{{ product.id }}"
                 data-name="{{ product.title | escape }}"
                 data-url="{{ shop.url }}{{ product.url }}"
                 data-image-url="{{ product.featured_image | product_img_url: "large" |replace: '?', '%3F' | replace: '&','%26'}}"
                 data-description="{{ product.description | escape }}">
            </div>
        </div>
    </div>
</div>
<span id="extole-zone-product_page"></span>
<script type="text/javascript">
    /*function swatchTrigger(e) {
            if (e.target.parentNode) e.target.parentNode.classList.toggle('active');
            if (e.target.id === 'swatch-1-firm') {
                const btn = document.getElementById('swatch-1-medium');
                if (btn && btn.parentNode) btn.parentNode.classList.remove('active');
            } else {
                const btn = document.getElementById('swatch-1-firm');
                if (btn && btn.parentNode) btn.parentNode.classList.remove('active');
            }
        }
        document.getElementById('swatch-1-firm').addEventListener('click', swatchTrigger);
        document.getElementById('swatch-1-medium').addEventListener('click', swatchTrigger);*/

    (function (c, b, f, k, a) {
        c[b] = c[b] || {};
        for (c[b].q = c[b].q || []; a < k.length;) f(k[a++], c[b])
    })(window, "extole", function (c, b) {
        b[c] = b[c] || function () {
            b.q.push([c, arguments])
        }
    }, ["createZone"], 0);
    extole.createZone({
        name: "product_page",
        element_id: "extole-zone-product_page",
        parameters: {}
    });
</script>

{{ 'swatch-new.js' | asset_url | script_tag }}

</div>
<script>
    mixpanel.track(
        "Product Viewed",
        {
            "name": "{{ product.title }}",
            "price": "{{ product.price | money_without_currency }}",
            "brand": "{{ product.vendor }}",
            "category": "{{ collection.title }}",
            "variant": "{% for variant in product.variants %}{{ variant.sku }}{% endfor %}"
        });
    $(document).ready(function () {
        $("#product-add-to-cart").click(function () {
            mixpanel.track("Added to Cart", { "name": "{{ product.title }}" });
        });
    });
</script>
